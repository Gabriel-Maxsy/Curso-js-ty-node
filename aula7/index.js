const nome = "Gabriel";

console.log(`O valor da const: ${nome}, Não pode ser mudado como no código abaixo (olhe o código)`);

// nome = "teste";
// E o valor da const deve ser atribuida no momento de sua criação.